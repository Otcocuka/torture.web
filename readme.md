Добро пожаловать в "Torture2"
Это Single Page Application (SPA), написанная на "чистом" Vanilla JavaScript (без фреймворков). Весь код находится в браузере, а данные хранятся в localStorage (память твоего браузера).

1. Структура проекта (Файлы)
Мы разделили код на логические части, чтобы его было легко поддерживать.

index.html:

Это скелет. Здесь нет логики, только HTML-разметка и подключение скриптов.
Внутри есть теги <div id="view-..."> — это наши "экраны" (Привычки, Таймер, Колесо, Статистика, TODO). По умолчанию они скрыты.
В самом низу подключаются JS файлы строго по порядку.
styles.css:

Это внешность. Здесь живут кастомные анимации (fade-in), стили для модалок и специфические вещи (календарь GitHub, Колесо). Основной дизайн сделан на TailwindCSS (подключен через CDN в HTML).
state.js (Мозг / База данных):

Это единственный источник правды. Объект Store содержит всё: привычки, настройки, историю колеса, статистику таймера.
У него есть два главных метода: load() (читать из localStorage) и save() (писать в localStorage).
Золотое правило: Если данные нужны "навсегда" (чтобы не пропали при обновлении), они идут сюда. Если данные временные (например, текст в поле ввода), они в UI.
logic.js (Машины / Процессы):

Здесь живут классы (Помидор, Колесо).
Эти классы не знают про HTML. Они считают время, решают, куда повернуть колесо, включают звук.
Когда им нужно сообщить миру о результате (например, "Время вышло!"), они вызывают внешние функции (из UI).
ui.js (Художник / Руководитель):

Это самая большая часть. Она связывает мозг и процессор с картинкой на экране.
render... функции: Генерируют HTML на основе данных из Store.
bind... функции: Вешают обработчики событий (клик, ввод текста) на кнопки и поля.
Когда пользователь нажимает "Start", UI говорит PomodoroController.start().
main.js (Директор / Сборщик):

Ждет, пока страница полностью загрузится (DOMContentLoaded).
Загружает данные (Store.load()).
Инициализирует "Машины" (new PomodoroController(), new WheelController()).
Запускает UI.init(), чтобы всё нарисовать и повесить события.
Запускает AppTracker для подсчета общего времени.
2. Жизненный цикл приложения (Как всё запускается)
Пользователь открывает страницу.
main.js:
Store.load() — пытается достать данные из localStorage. Если ничего нет, предлагает сделать бэкап.
AppTracker.init() — запускает таймер общего времени (даже если просто сидеть на странице).
window.Controllers = { ... } — создает экземпляры классов логики.
UI.init() — это главная функция старта.
UI.init():
Вешает слушатели на кнопки навигации.
Вызывает bindHabitsEvents, bindTimerEvents и т.д. (навешивает обработчики на кнопки "Добавить", "Старт", "Сбросить").
Вызывает renderHabits() (рисует список привычек на старте).
Вызывает updateStats() (обновляет цифры в статистике).
Пользователь взаимодействует:
Клик по кнопке -> UI ловит событие -> вызывает метод Logic (например, start()) -> Logic начинает считать -> Logic вызывает UI.updateTimerDisplay() -> Пользователь видит цифры.
3. Детальное описание ключевых модулей
📦 Хранилище (Store)
Это простой объект с методами. Вся логика работы с localStorage спрятана внутри.

Почему так? Чтобы не писать JSON.parse(...) и JSON.stringify(...) в 10 разных местах.
Техника: Мы используем "слабое слияние" ({ ...default, ...loaded }). Это значит, что если мы обновим приложение и добавим новое поле (например, appStats), старые данные из браузера не сломаются — старые поля останутся, а новые получат значения по умолчанию.
⚙️ Логика (Logic)
PomodoroController:

У него есть внутреннее состояние (this.state): isRunning, timeLeft, endTime.
Он не обновляет DOM сам. Он использует UI.Timer.updateDisplay(). Это важно: логика отделена от представления.
Он работает через setInterval (каждую секунду), но точнее считает через разницу времени (Date.now()), чтобы компенсировать "засыпание" вкладки.
WheelController:

Хранит список активностей.
Метод spin() — это Promise-ориентированный подход.
Мгновенно выбирает победителя.
Мгновенно сохраняет в Store (гарантия данных).
Запускает визуальную анимацию (requestAnimationFrame).
Ключевой момент: Данные пишутся до того, как анимация закончится. Если браузер упадет посередине вращения — результат уже сохранен.
🎨 Интерфейс (UI)
Рендеринг (Render):

Мы используем интерполяцию строк (backticks `...`) для генерации HTML. Это быстро и понятно.
Например, renderHabits() берет массив Store.data.habits, делает .map(), превращая каждый объект в кусок HTML-кода, и вставляет готовую строку в innerHTML.
Важно: Вся вставка идет одной командой. Это защищает от "утечек" памяти, так как старые элементы удаляются полностью.
События (Events):

Мы используем Делегирование событий (особенно в Kanban и списке привычек).
Вместо того чтобы вешать onclick на каждую карточку (их может быть 100), мы вешаем один обработчик на родительский контейнер (#habitsList, #kanbanBoard).
Внутри проверяем: if (e.target.dataset.action === 'increment') .... Это экономит память и работает для элементов, которые добавляются динамически.
Модальные окна:

Это временные HTML-строки, которые создаются на лету и вставляются в <div id="modalContainer">.
При закрытии (клик или Escape) мы добавляем CSS-классы для анимации (scale-95, opacity-0), ждем 200мс (время анимации) и удаляем элемент из DOM (remove()).
Drag and Drop (Kanban):

Это стандартный HTML5 API.
dragstart: Запоминаем ID карты в dataTransfer (специальное хранилище события).
dragover: Над колонкой. Обязательно e.preventDefault(), иначе сброс не сработает. Добавляем визуальный эффект.
drop: Получаем ID карты, получаем ID новой колонки, меняем данные в Store, перерисовываем доску.
4. Несколько неочевидных фишек (Pro Tips)
Управление временем (Uptime):

При старте (main.js) мы запоминаем Date.now().
Каждые 5 секунд тикер считает разницу и добавляет её к totalUptime в Store.
Если ты перезагрузишь страницу, тикер начнет считать заново, но старые накопленные секунды уже записаны в localStorage. Итог: время не теряется.
Аудио без файлов:

В logic.js (AudioEngine) мы используем window.AudioContext. Это генератор звука прямо в браузере (синусная волна). Не нужно качать mp3 файлы.
График привычек:

В статистике рисуется сетка. Самое интересное: transform: scale-y-[-1] и direction: rtl. Это заставляет Grid рисоваться снизу-вверх (свежие даты внизу, старые вверху), как в GitHub, но сохраняет привычный порядок колонок слева-направо. Это чистый CSS-хак.
Кнопка "Нет" и Escape:

Мы повесили глобальные обработчики на document. Это значит, что даже если кнопка "Нет" появилась динамически (в модалке), она сработает, потому что JavaScript проверяет "клик по любому месту документа", а потом смотрит, есть ли у кнопки атрибут data-close-modal.








__________________________________________________




Добро пожаловать в "Torture2"
Это Single Page Application (SPA), написанная на "чистом" Vanilla JavaScript (без фреймворков). Весь код работает в браузере, а данные хранятся в localStorage.

📁 Структура проекта
Мы разделили код на логические файлы, чтобы его было легко поддерживать.

index.html — Скелет.

Здесь только HTML-разметка и подключение скриптов.
Содержит <div id="view-..."> — наши "экраны" (вкладки). По умолчанию скрыты.
Скрипты подключаются строго по порядку.
styles.css — Внешность.

Кастомные анимации (fade-in), стили модалок, специфика (календарь GitHub, Колесо).
Основной дизайн — TailwindCSS (через CDN в HTML).
state.js — Мозг / База данных.

Объект Store содержит всё: привычки, настройки, историю, статистику.
Главные методы: load() (читать из localStorage) и save() (писать).
Золотое правило: Данные, которые должны сохраняться навсегда, идут сюда.
logic.js — Процессы / "Повар".

Живут классы: PomodoroController, WheelController, AudioEngine.
Они не знают про HTML. Они считают время, решают задачи, включают звук.
Когда результат готов, они вызывают внешние функции (из UI).
ui.js — Интерфейс / "Официант".

Самый большой файл. Связывает мозг с картинкой.
render... — генерирует HTML из данных Store.
bind... — вешает обработчики кликов и ввода.
Когда пользователь жмет "Start", UI передает команду в Logic.
main.js — Директор / Сборщик.

Ждет загрузки страницы (DOMContentLoaded).
Загружает данные (Store.load()).
Инициализирует "Машины" (new PomodoroController(), new WheelController()).
Запускает UI.init(), AppTracker.
🔄 Жизненный цикл приложения
Открытие страницы.
main.js:
Store.load() — восстанавливает данные.
AppTracker.init() — запускает таймер общего времени.
Создает экземпляры классов логики.
UI.init() — главная точка входа.
UI.init():
Навешивает слушатели на кнопки навигации.
Вызывает bindHabitsEvents, bindTimerEvents и т.д.
Вызывает renderHabits() и updateStats() для первичного рендера.
Пользователь взаимодействует:
Клик -> UI ловит -> вызывает Logic -> Logic считает -> UI обновляет картинку.
📦 Модули в деталях
Хранилище (Store)
Объект с методами работы с localStorage. Вся логика спрятана, чтобы не дублировать код.

Техника: Слабое слияние { ...default, ...loaded } гарантирует, что обновление приложения не сломает старые данные.
Логика (Logic)
PomodoroController:

Имеет внутреннее состояние (isRunning, timeLeft).
Не трогает DOM. Использует UI.Timer.updateDisplay().
Считает время через Date.now() - endTime (выживает при "засыпании" вкладки).
WheelController:

Метод spin() возвращает Promise.
Логика: Выбор результата -> Сохранение в Store -> Анимация.
Данные пишутся мгновенно, гарантируя сохранность даже при обновлении страницы.
Интерфейс (UI)
Рендеринг:

Используем интерполяцию строк (backticks) и .map() для генерации HTML.
Вставка innerHTML происходит одной командой, что эффективно.
События (Делегирование):

Вешаем один обработчик на родительский контейнер (например, #habitsList).
Внутри проверяем e.target.dataset.action. Это работает для динамических элементов.
Модальные окна:

Генерируются на лету. Закрытие (клик или Escape) сначала запускает анимацию (opacity: 0), а затем удаляет элемент из DOM через setTimeout.
Drag and Drop (Kanban):

dragstart — запоминает ID карты.
dragover — e.preventDefault() (обязательно!) + визуальный эффект.
drop — меняет данные в Store и перерисовывает доску.
🪄 Неочевидные фишки
Uptime Tracker (main.js):

Считает время, пока страница открыта (даже в фоне).
При закрытии или уходе со страницы (visibilitychange, beforeunload) сохраняет накопленные секунды в Store.
Аудио без файлов (logic.js):

AudioEngine использует window.AudioContext для генерации бипов (синусная волна). Никаких mp3.
График привычек (ui.js):

CSS-хак: transform: scale-y-[-1] + direction: rtl заставляет Grid рисоваться снизу-вверх (как в GitHub), но сохраняет нормальный порядок колонок.
Глобальные кнопки закрытия:

document.addEventListener('click', ...) находит кнопки с data-close-modal в любой части страницы.
document.addEventListener('keydown', ...) на Escape закрывает любую открытую модалку.


+++++++++++++++++++++++++++++++++++++++++++++++++++




Пользователь → UI (ловит) → Logic (решает) → Store (меняет) → UI (рисует).
Это однонаправленный поток данных. Данные идут по кругу: View → Logic → Model → View



Самое точное название — "Flux-lite" или "Модифицированный MVC".

Но в индустрии для такого кода на Vanilla JS чаще говорят просто: "Чистая архитектура" (Clean Architecture) или "Разделение ответственности" (Separation of Concerns). Главное, что здесь нет спагетти-кода, и каждый файл знает только о своей зоне ответственности.

Кратко для резюме: "Архитектура, основанная на потоке данных: Store (Model) ↔ Logic (Business Logic) ↔ UI (View/Controller)".







todo:
second brain features=>learning module(books first, then other sources):
-first adding the source-done
-tracking inside module(time spent, pages, etc.)- almost 





-llm comments on that(late stage, with api)- next todo
-quizes after reading(hooks on start and end of reading, simple knowledge-check questions like on w3)-next todo




bl: 

-canban for effective telegram revision asap(with fields for materials, resources,etc.)-done, can start revision
-connection between canban cards()
happy new year everyone


happy new year!

gl finishing it before jan 1st - gj, ez 


